import PrivacyPolicyPage, { metadata as privacyMetadata } from '../privacy/page';

export const metadata = {
  ...privacyMetadata,
  alternates: {
    canonical: 'https://chemlearn-67.web.app/privacy',
  },
};

export default PrivacyPolicyPage;
